# trip-planner (planned LangGraph breadth example)

This example is reserved for the second-agent breadth demo: flights, weather,
and calendar tools under flapping side effects and schema-drift faults.

Current status: planned after the support-bot harden loop is live-verified.
